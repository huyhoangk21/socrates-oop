package com.hoangle;

public class Bird extends Animal {

    public Bird(String color, int leg_number) {
        super(color, leg_number);
    }

    public Bird(String color, int leg_number, boolean can_fly) {
        super(color, leg_number, can_fly);
    }
}
