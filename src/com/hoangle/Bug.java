package com.hoangle;

public class Bug extends Animal {

    public Bug(String color, int leg_number) {
        super(color, leg_number);
    }

    public Bug(String color, int leg_number, boolean can_fly) {
        super(color, leg_number, can_fly);
    }
}
